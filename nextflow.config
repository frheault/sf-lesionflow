def _algo_file = new File("${projectDir}/lib/AlgorithmSelection.groovy")
if (!_algo_file.exists()) {
    _algo_file = new File("lib/AlgorithmSelection.groovy")
}
if (_algo_file.exists()) {
    AlgorithmSelection = new GroovyClassLoader(getClass().getClassLoader()).parseClass(_algo_file)
}

includeConfig 'conf/base.config'
includeConfig 'conf/qc.config'

params {
    input                   = false
    mni_template            = false
    fs_license              = false
    participant_label       = false
    output                  = "results"
    algorithms              = false
    skip_algorithms         = false
    max_memory              = false
    max_forks               = false
    use_gpu                 = false
    synthstrip_gpu          = false
    sif_cache               = false
    cluster_gpu_options     = false
    staple_threshold        = 0.90
    staple_min_cluster_size = 6
    staple_min_distance     = 3
    staple_gaussian_sigma   = 0.8
    pct_change_threshold    = 20.0
}

profiles {
    docker {
        docker.enabled          = true
        conda.enabled           = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
        docker.runOptions       = '-u $(id -u):$(id -g) -e HOME=/tmp --entrypoint "" --shm-size 2g'
    }
    apptainer {
        apptainer.enabled       = true
        apptainer.autoMounts    = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        // Several third-party images (e.g. emorycn2l/emory_robust_wmh) assume
        // a Docker-style writable container filesystem and mkdir/write to
        // paths baked into the image itself (e.g. /app/inputs/preproc).
        // Apptainer's SIF is read-only by default, unlike Docker -- an
        // in-memory writable overlay avoids "Read-only file system" crashes
        // without needing to rebuild the image or bind-mount every such path.
        apptainer.runOptions    = '-B /tmp --writable-tmpfs'
        cacheDir                = "${params.sif_cache ?: (System.getenv('NXF_APPTAINER_CACHEDIR') ?: "${projectDir}/singularity_cache")}"
    }
    singularity {
        singularity.enabled     = true
        singularity.autoMounts  = true
        conda.enabled           = false
        docker.enabled          = false
        apptainer.enabled       = false
        singularity.runOptions  = '-B /tmp --writable-tmpfs'
        cacheDir                = "${params.sif_cache ?: (System.getenv('NXF_SINGULARITY_CACHEDIR') ?: "${projectDir}/singularity_cache")}"
    }
    gpu {
        params.use_gpu          = true
        docker.runOptions       = '-u $(id -u):$(id -g) -e HOME=/tmp --entrypoint "" --shm-size 2g --gpus all'
        apptainer.runOptions    = '--nv -B /tmp --writable-tmpfs'
        singularity.runOptions  = '--nv -B /tmp --writable-tmpfs'
    }

    local_dev {
        includeConfig 'conf/local_dev.config'
    }
    hpc {
        includeConfig 'conf/hpc.config'
    }
    offline {
        includeConfig 'conf/offline.config'
    }
    stub {
        docker.enabled          = false
        singularity.enabled     = false
        apptainer.enabled       = false
        process.container       = null
    }
}

manifest {
    name            = 'sf-lesionflow'
    description     = 'Reproducible Nextflow pipeline for Multiple Sclerosis lesion segmentation and longitudinal harmonization.'
    version         = '1.0.0'
    nextflowVersion = '!>=24.04.0'
}

// NOTE: env{} and process.shell overrides (PYTHONNOUSERSITE, OPENBLAS_NUM_THREADS,
// strict bash flags) were deliberately held back here -- both are GLOBAL process
// directives that would invalidate the -resume cache hash for every already-completed
// task in the pipeline, not just new ones. Apply them in a dedicated run where a full
// recompute is acceptable, not bundled into an unrelated single-process fix.


process {
    publishDir = {
        def base       = "${params.output}/${meta?.subject ?: subject ?: 'pipeline_info'}"
        def sessionDir = meta ? "/${meta.session ?: 'ses-single'}" : ''
        "${base}${sessionDir}/${task.process.replaceAll(':', '-')}"
    }

    withName: "CONSENSUS_STAPLE" {
        ext.threshold        = { params.staple_threshold }
        ext.min_cluster_size = { params.staple_min_cluster_size }
        ext.min_distance     = { params.staple_min_distance }
        ext.gaussian_sigma   = { params.staple_gaussian_sigma }
        publishDir = [
            path: { "${params.output}/${meta.subject}/${meta.session ?: 'ses-single'}/staple_classical" },
            mode: 'copy',
            pattern: '*.nii.gz'
        ]
    }

    withName: "HARMONIZATION_STAPLE" {
        ext.min_cluster_size     = { params.staple_min_cluster_size }
        ext.min_distance         = { params.staple_min_distance }
        ext.gaussian_sigma       = { params.staple_gaussian_sigma }
        ext.pct_change_threshold = { params.pct_change_threshold }
        publishDir = [
            path: { "${params.output}/${subject}" },
            mode: 'copy'
        ]
    }

    withName: "RESAMPLE_T1" {
        ext.voxel_size   = "1.0 1.0 1.0"
        ext.first_suffix = "t1"
    }

    withName: "RESAMPLE_FLAIR" {
        ext.voxel_size   = "1.0 1.0 1.0"
        ext.first_suffix = "flair"
    }

    withLabel: 'process_gpu' {
        ext.gpu = { params.use_gpu }
    }

    withName: "SYNTHSTRIP_T1|SYNTHSTRIP_FLAIR" {
        ext.nocsf  = true
        ext.prefix = { "${meta.id}_${task.process.contains('T1') ? 't1' : 'flair'}" }
    }

    withName: "N4_T1" {
        ext.shrink_factor = 3
        ext.prefix        = { "${meta.id}_t1" }
    }

    withName: "N4_FLAIR" {
        ext.shrink_factor = 3
        ext.prefix        = { "${meta.id}_flair" }
    }

    withName: "MASK_T1" {
        ext.prefix = { "${meta.id}_t1" }
    }

    withName: "MASK_FLAIR" {
        ext.prefix = { "${meta.id}_flair" }
    }

    withName: "CROP_T1_MASK" {
        ext.output_bbox  = true
        ext.first_suffix = "t1_mask"
    }

    withName: "CROP_T1_RAW" {
        ext.first_suffix = "t1_raw"
    }

    withName: "CROP_FLAIR_RAW" {
        ext.first_suffix = "flair_raw"
    }

    withName: "CROP_FLAIR_MASK" {
        ext.output_bbox  = true
        ext.first_suffix = "flair_mask"
    }

    withName: "REGISTER_FLAIR_TO_T1" {
        ext.quick     = true
        ext.transform = 'r'
        ext.prefix    = { "${meta.id}_flair_to_t1" }
    }

    withName: "REGISTER_T1_TO_BASELINE" {
        ext.quick     = true
        ext.transform = 'r'
        ext.prefix    = { "${meta.id}_t1_to_baseline" }
    }

    withName: "REGISTER_BASELINE_TO_MNI" {
        ext.quick     = true
        ext.transform = 'a'
        ext.prefix    = { "${meta.id}_baseline_to_mni" }
    }

    withName: "TRANSFORM_T1W_TO_MNI" {
        ext.prefix = { "${meta.id}_t1" }
        ext.suffix = "space-MNI"
        publishDir = [
            path: { "${params.output}/${meta.subject}/${meta.session ?: 'ses-single'}" },
            mode: 'copy',
            pattern: '*.nii.gz'
        ]
    }

    withName: "TRANSFORM_FLAIR_TO_MNI" {
        ext.prefix = { "${meta.id}_flair" }
        ext.suffix = "space-MNI"
        publishDir = [
            path: { "${params.output}/${meta.subject}/${meta.session ?: 'ses-single'}" },
            mode: 'copy',
            pattern: '*.nii.gz'
        ]
    }

    withName: "TRANSFORM_T1W_UNSTRIPPED_TO_MNI" {
        ext.prefix = { "${meta.id}_t1" }
        ext.suffix = "unstripped_space-MNI"
        publishDir = [
            path: { "${params.output}/${meta.subject}/${meta.session ?: 'ses-single'}" },
            mode: 'copy',
            pattern: '*.nii.gz'
        ]
    }

    withName: "TRANSFORM_FLAIR_UNSTRIPPED_TO_MNI" {
        ext.prefix = { "${meta.id}_flair" }
        ext.suffix = "unstripped_space-MNI"
        publishDir = [
            path: { "${params.output}/${meta.subject}/${meta.session ?: 'ses-single'}" },
            mode: 'copy',
            pattern: '*.nii.gz'
        ]
    }

    withName: "SEGMENTATION_LST_AI" {
        ext.when = { AlgorithmSelection.isActive('lst_ai', params) }
    }

    withName: "SEGMENTATION_SAMSEG" {
        ext.when = { AlgorithmSelection.isActive('samseg', params) }
    }

    withName: "SEGMENTATION_WMH_SYNTHSEG" {
        // cpus/memory/time are owned by conf/base.config (single source of truth).
        containerOptions = { (workflow.containerEngine == 'docker') ? "-e HOME=${task.workDir}" : null }
        ext.when         = { AlgorithmSelection.isActive('wmh_synthseg', params) }
    }


    withName: "SEGMENTATION_FAST_OUTLIER" {
        ext.when = { AlgorithmSelection.isActive('fast_outlier', params) }
    }

    withName: "SEGMENTATION_FLAMES" {
        ext.when = { AlgorithmSelection.isActive('flames', params) }
    }

    withName: "SEGMENTATION_TRUENET" {
        ext.when = { AlgorithmSelection.isActive('truenet', params) }
    }

    withName: "SEGMENTATION_HYPERMAPP3R" {
        // hypermapper's own cosmetic sign-off print calls pwd.getpwuid(os.getuid()),
        // which hard-crashes (KeyError) because the arbitrary Docker UID has no
        // /etc/passwd entry inside the container -- this happens AFTER the real
        // segmentation already succeeded. Bind-mounting the HOST's /etc/passwd
        // (prior attempt) does NOT fix this on hosts where accounts are resolved
        // via a directory service (sssd/AD) rather than the flat file -- the file
        // itself has no line for this UID either. Resolve the entry via `getent`
        // (which does hit the directory service) on the host before the container
        // starts, write it to a real file in the task work dir, and mount that
        // instead -- avoids running as root (would reintroduce root-owned files).
        // Note: Only needed for Docker; Apptainer/Singularity dynamically populates
        // /etc/passwd with the host user credentials and does not accept Docker's -v flag.
        beforeScript     = { (workflow.containerEngine == 'docker') ? 'getent passwd $(id -u) > .container_passwd' : '' }
        containerOptions = { (workflow.containerEngine == 'docker') ? "-v ${task.workDir}/.container_passwd:/etc/passwd:ro" : null }
        ext.when         = { AlgorithmSelection.isActive('hypermapp3r', params) }
    }

    withName: "SEGMENTATION_SEGCSVD" {
        ext.when = { AlgorithmSelection.isActive('segcsvd', params) }
    }

    withName: "SEGMENTATION_EMORY_ROBUST" {
        ext.when = { AlgorithmSelection.isActive('emory_robust', params) }
    }

    withName: "SEGMENTATION_MARS_WMH" {
        ext.when = { AlgorithmSelection.isActive('mars_wmh', params) }
    }

    withName: "SEGMENTATION_BAWIL" {
        ext.when = { AlgorithmSelection.isActive('bawil', params) }
    }

    withName: "SEGMENTATION_MIMOSA" {
        ext.when = { AlgorithmSelection.isActive('mimosa', params) }
    }

    withName: "SEGMENTATION_SHIVAI" {
        ext.when = { AlgorithmSelection.isActive('shivai', params) }
    }
}

report {
    overwrite = true
}

timeline {
    overwrite = true
}

trace {
    overwrite = true
}

